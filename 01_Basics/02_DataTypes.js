"use strict" // treats all JS code as newer version

// alert (3 + 3) // We are using NodeJS and not browser console

console.log( 3 
    +
    3)      // Code Readability should be high.

console.log("Shivam");

let name = "Neha"
let age = 28
let isLoggedIn = false
let city;
let state = null

// number => 2^53
// bigint
// string => ""
// boolean => true/false
// null => standalone value ,in short it's a representaiton of empty value.
// undefined 
// symbol => unique

// The typeof() operator in JavaScript is used to check the data type of a variable or value.

console.log(typeof(name));
console.log(typeof(age));
console.log(typeof(isLoggedIn));
console.log(typeof(undefined));   // undefined
console.log(typeof(null));        // object



